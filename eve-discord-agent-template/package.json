{
  "name": "eve-discord-agent-template",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "pnpm --dir services/relay build && pnpm --dir services/eve build",
    "typecheck": "pnpm --dir services/relay exec tsc --noEmit && pnpm --dir services/eve typecheck"
  }
}
